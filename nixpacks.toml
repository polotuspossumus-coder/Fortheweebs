[phases.setup]
nixPkgs = ['nodejs-20_x']

[phases.install]
cmds = [
  'echo "Installing only production dependencies for Express server"',
  'npm ci --omit=dev --ignore-scripts'
]

[phases.build]
cmds = [
  'echo "No build step needed - using server.js directly"'
]

[start]
cmd = 'node server.js'
